name: easy_english
description: An application for learning English.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_auth: ^0.15.5+3
  cloud_firestore: ^0.13.4+2
  provider: ^5.0.0
  flutter_spinkit: ^5.0.0
  random_string: ^2.1.0
  rflutter_alert: ^2.0.2
  flutter_speed_dial: ^3.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

